const { Observer, Rotation_HOR_ECL, VectorFromHorizon, RotateVector, SphereFromVector, HorizonFromVector, Rotation_ECL_HOR } = require('astronomy-engine'); const dateUTC = new Date(Date.UTC(1986, 8, 17, 15, 42)); const obs = new Observer(8.432, 99.968, 0); const horSphere = { lat: 0, lon: 90, dist: 1 }; const horVec = VectorFromHorizon(horSphere, dateUTC, ""); console.log('horVec', JSON.stringify(horVec)); const horSphere2 = HorizonFromVector(horVec, ""); console.log('horizon sphere from vec', JSON.stringify(horSphere2)); const rot = Rotation_HOR_ECL(dateUTC, obs); const eclVec = RotateVector(rot, horVec); const eclSphere = SphereFromVector(eclVec); console.log('eclSphere', JSON.stringify(eclSphere)); const rotBack = Rotation_ECL_HOR(dateUTC, obs); const horVecBack = RotateVector(rotBack, eclVec); const horSphereBack = HorizonFromVector(horVecBack, ""); console.log('back horizon', JSON.stringify(horSphereBack));